[package]
name = "statute-version-control"
version = "0.1.0"
edition = "2021"
description = "Example: Statute version control, snapshots, and backup with legalis-registry"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
legalis-core = { path = "../../crates/legalis-core" }
legalis-registry = { path = "../../crates/legalis-registry" }

tokio = { version = "1", features = ["full"] }
